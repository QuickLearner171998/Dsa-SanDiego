To do:
1) Replace model_main.py in object_detection dir
2) Replace model_lib.py in object_detection dir
3) Replace generate_tfrecord.py 
3) Remove existing training\ 
4) Move training dir to object_detection

For training:

1) In object_detection dir run:-
python generate_tfrecord.py --annotations=path\to\train_dir --output_path=train.record
python generate_tfrecord.py --annotations=path\to\val_dir --output_path=test.record

This will create train and test records in object_detection directories

2) 